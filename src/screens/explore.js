import { View, Text } from 'react-native';

export default ExploreScreen = (props) => {
    return (
        <View style={{ flex: 1, justifyContent: 'center' }}>
            <Text>Explore Screen</Text>
        </View>
    );
};
